#!/usr/bin/bash

[ -z $1 ] && exit

DIR=$HOME/.config/config-schemes

SCHEME=$1

MOVES=$(cat $DIR/moves)

for LOC in $MOVES; do
    SRC=$DIR/schemes/$SCHEME/$(echo $LOC | cut -d '=' -f 1)
    DEST=$HOME/$(echo $LOC | cut -d '=' -f 2)

    cp $SRC $DEST
done

$HOME/.config/bspwm/bspwmrc
